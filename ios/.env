# Firebase App Distribution Configuration
# Find Firebase App ID in: Firebase Console > Project Settings > Your apps > iOS app
# Format: 1:PROJECT_ID:iOS:APP_ID

# Required: Firebase iOS App ID
# Replace with your actual Firebase App ID from Firebase Console
FIREBASE_APP_ID=1:359434805690:ios:bcc77de90cfd9076cc7d83

# Optional: Individual testers (comma-separated email addresses)
FIREBASE_TESTERS=jsun.vu@hailstonelab.com,duongmobiledev@gmail.com

# Optional: Tester groups (comma-separated group names)
# Use "qa" (lowercase) as shown in Firebase Console
FIREBASE_TESTER_GROUPS=qa

# Optional: Custom release notes
# If not set, will use default: "Staging build v{version} ({code})"
# FIREBASE_RELEASE_NOTES=

# Optional: Service Account Credentials File (for CI/CD only)
# Only needed if not using Firebase CLI authentication
# FIREBASE_SERVICE_CREDENTIALS_FILE=/path/to/service-account.json
